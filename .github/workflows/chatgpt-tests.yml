name: ChatGPT Test Run

on:
  push:
    branches:
      - chatgpt-test-20260906

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONPATH: .
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run pytest suite
        run: pytest -q
      - name: Run short-horizon five-method comparison
        run: python scripts/chatgpt_nested_holding_comparison.py
      - name: Upload short-horizon comparison CSVs
        uses: actions/upload-artifact@v4
        with:
          name: nested-holding-comparison
          path: |
            nested_holding_summary.csv
            nested_holding_detail.csv
            nested_holding_t_diagnostics.csv
            nested_holding_latest_weights.csv
            nested_holding_cagr_table.csv
            nested_holding_sharpe_table.csv
            nested_holding_winners.csv
